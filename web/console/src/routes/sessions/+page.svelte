<script lang="ts">
  // Phase 6 will surface live session telemetry. This is a placeholder so
  // the nav resolves and operators can deep-link until the data lands.
</script>

<header class="page-head">
  <h1>Sessions</h1>
</header>

<p class="muted">
  Session inspection lands in Phase 6. This page will show active gateway sessions, the tools they
  invoked, and a replay timeline.
</p>

<style>
  .page-head {
    margin-bottom: var(--space-6);
  }
  h1 {
    margin: 0;
    font-size: var(--text-2xl);
    font-weight: var(--weight-semibold);
  }
  .muted {
    color: var(--color-text-muted);
  }
</style>
