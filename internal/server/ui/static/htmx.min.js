/* Portico — htmx placeholder.
 *
 * Real htmx is not yet vendored. The placeholder exists so /static/htmx.min.js
 * returns 200 and the Phase 0 smoke check passes. When real UI components land
 * (Phase 3+), vendor htmx 2.x from https://htmx.org/ and replace this file.
 *
 * Until then, the Console is fully static — no client-side interactivity is
 * required for the placeholder pages.
 */
;(function(){ /* no-op */ })();
